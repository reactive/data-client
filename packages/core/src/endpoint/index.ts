export type {
  FetchShape,
  ReadShape,
  MutateShape,
  DeleteShape,
} from '@rest-hooks/core/endpoint/shapes';
export type {
  SetShapeParams,
  ParamsFromShape,
  OptimisticUpdateParams,
  SchemaFromShape,
  BodyFromShape,
  ReturnFromShape,
} from '@rest-hooks/core/endpoint/types';
